## This document is not for agent read.

## TODO:
1. при выполнении `/init-requirements` агент должен проверить наличие существующих `init/**` и предложить варианты продолжения:  
    А. Отправить текущие requirements в архив, проревьюить старые, создать на их основе новые.
    Б. Утилизировать текущие requirements и собрать новые intentions.
    В. Остановиться и оъяснить что делать дальше (возможно ошибка пользователя).
2. ~~при выполнении `/implement-task` задачи вплоть до 07 выводили ответ на русском, далее начали выводить ответ на английском. надо проверить консистентность выводов.~~
3. ~~результат выполнения должен также сбрасывать краткий userguide в readme проекта по использованию нового функционала.~~
4. ~~таски должны быть разделены на feature и bug, а не просто task.~~
5. ~~при прогонах должны заполняться некие своды багов на прогонах, вероятно с отметкой даты прогона.~~
6. ~~каждый баг должн быть взят в работу отдельной "командой\скиллом". по результатам должен быть прогон (если нужно), анализ недостатка в спецификации, добавление ADR по необходимости, внесение изменений в спеку, затем surgical изменения в коде по diff от спеки.~~
7. ~~текущий фреймвор работы через спецификацию нужно назвать DeltaFuse (репозиторий delta-fuse), необходимо добавить документацию для использования фреймворка delta-fuse.~~
8. ~~если в рамках реализации\исправления бага спека не трогалась, то нужно отдельно юзеру об этом сообщить в ответе~~
9. ~~проверить, что текущий процесс по имплементации (фиксации багов) сперва подразумевает написание тестов, проверка что эти тесты *не проходят* на текущей реализации (нет смысла писать тесты которые уже работают без фиксов), а затем уже реализует в коде.~~
10. есть ли смысл отдельно в доках хранить описание тест-кейсов, или достаточно комментариев в коде?
