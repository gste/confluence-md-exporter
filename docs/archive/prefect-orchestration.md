# Prefect-оркестрация — исторический снимок

Текущая поставка — однопоточный локальный скрипт выгрузки без Prefect. Снимок реализации с Prefect 3.x (`prefect>=3,<4`, flow, ephemeral API, markdown-артефакты UI, `EXPORT_CONCURRENCY`) сохранён git-тегом `history/prefect-orchestration` (коммит `c2af027`).

Вернуть оркестрацию — отдельное улучшение: тег, не текущий `docs/spec/`.
